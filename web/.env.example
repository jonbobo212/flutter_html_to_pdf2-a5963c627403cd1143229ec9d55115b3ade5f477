# Server-only — shared ecosystem Supabase (vitrina_ tables)
SUPABASE_URL=https://mywdtmimiazeyhfdtqlw.supabase.co
SUPABASE_SERVICE_ROLE_KEY=

# Tenant subdomains resolve against this root (bus row #44 decision)
NEXT_PUBLIC_ROOT_DOMAIN=aspira.study

# Admin v1: token-gated links (HMAC of tenant slug) — Phase 1 moves to Supabase auth
VITRINA_ADMIN_SECRET=

# Translate-on-save (Haiku via shared AI cache); CMS degrades gracefully without it
ANTHROPIC_API_KEY=
# VITRINA_TRANSLATE_MODEL=claude-haiku-4-5
